{
  "name": "band-management-tool",
  "version": "1.0.0",
  "description": "Band Management Tool - Manage your band's rehearsals, events, and schedules",
  "main": "index.html",
  "scripts": {
    "start": "python3 -m http.server 8000",
    "dev": "browser-sync start --server --files \"index.html, css/*.css, js/*.js\" --no-notify --watch",
    "install-capacitor": "npm install @capacitor/core @capacitor/cli @capacitor/ios",
    "init-capacitor": "npx cap init",
    "add-ios": "npx cap add ios",
    "sync": "npx cap sync",
    "open-ios": "npx cap open ios",
    "export-web": "mkdir -p www && cp index.html privacy.html manifest.json sw.js www/ && cp -R css js images assets icons www/",
    "ios-sync": "npm run export-web && npx cap sync ios && npx cap open ios"
  },
  "keywords": [
    "band",
    "planning",
    "rehearsal",
    "music"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@capacitor/cli": "^8.0.2",
    "browser-sync": "^3.0.4"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  }
}